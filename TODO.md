# TireStock — задачи проекта

Живой трекер. Отмечать по мере выполнения: `[ ]` → `[x]`.
Детали архитектуры — `docs/ARCHITECTURE.md`, обзор — `docs/PROJECT_OVERVIEW.md`.
Легенда: 🔴 блокер (ждём данные/доступ) · 🟡 в работе · ⏳ бэклог.

---

## ✅ Готово

### Фундамент
- [x] Монорепо: `web/` (Next.js+TS+Tailwind), `api/` (Go), `deploy/` (docker-compose)
- [x] Дизайн-токены из DESIGN_SYSTEM.md (`web/styles/tokens.css`, `@theme`)
- [x] UI-kit по DESIGN_SYSTEM.md + демо `/dev/ui`
- [x] Manrope self-hosted (preload + font-display:swap)
- [x] OpenAPI-контракт (`api/openapi.yaml`) → генерация TS-типов
- [x] Go: плоские фичи `catalog` + `orders`, единый формат ошибок
- [x] Интерфейсы-адаптеры `CatalogSource`, `OrderDelivery` + моки (12 реальных шин)
- [x] Postgres: pgx+sqlc+golang-migrate, миграция products/orders/outbox
- [x] Outbox-воркер (SKIP LOCKED, экспоненциальные ретраи) + тесты
- [x] Тесты: URL→SQL маппинг фильтров, воркер, доставка

### Витрина (по макету Figma)
- [x] Главная (hero + поиск), Header/Footer, breadcrumbs
- [x] Каталог (список + фильтры в URL query params)
- [x] Карточка товара (`/catalog/[slug]`, React.cache для метаданных)
- [x] Корзина (демо-состав)
- [x] Услуга «Хранение» + рабочая форма (server action → outbox)
- [x] Адаптив (мобильный reflow вместо наложения карточек)
- [x] Аудиты Vercel-skills + рефакторинг (buttonStyles, Stepper, catalog-options)

### Интеграция tradesk — Вариант 1 (мост)
- [x] Разведка tradesk (read-only): роуты, backcall-статусы, `/backcall/change`
- [x] Вскрыт контракт форм старого сайта (`/ajax/call.php`, `/ajax/request.php`)
- [x] Адаптер `integrations/oldsite` (мост через `call.php`) + тесты
- [x] Env-переключатель доставки в `main.go`: OLDSITE → TRADESK → мок
- [x] **Проверено end-to-end на боевом tradesk** (тестовая заявка долетела в «Обратный звонок»)

### Админка — Фаза 1 (каркас + Заказы + Товары)
- [x] Авторизация: многопольз. сессии в БД (pbkdf2-хеш, httpOnly-кука, Bearer в Go)
- [x] Миграция `0002`: `admin_users`, `admin_sessions`, `product_overrides`
- [x] Пакет `internal/admin` (login/logout/me, session-middleware, оверрайды) + тесты
- [x] Сид первого пользователя из env (`ADMIN_BOOTSTRAP_*`)
- [x] Тёмный сайдбар-каркас + `middleware.ts`-гард + route-группа `(storefront)`
- [x] Экран «Заказы» (проекция outbox, табы, «Повторить»)
- [x] Детальная карточка заказа `/admin/orders/[id]` (28.08): покупатель, комментарий (способ получения/адрес), состав, статус доставки в tradesk (наш outbox) + **ЖИВОЙ статус заказа ИЗ tradesk** (обратная интеграция `/data/status` по order_code) — «Новый/В работе/Готов/Выдан» + полоса прогресса. Кнопка «Подробнее» в списке. Проверено сквозняком (заказ #28 → C288416 → статус «Новый»)
- [ ] 🔴 Тянуть ВСЕ заказы магазина из tradesk (не только с сайта) — нужен вход менеджера tradesk (`TRADESK_USERNAME/PASSWORD`, сейчас НЕ заданы) → логин в веб-админку Yii2 + чтение списка. Наш клиент умеет `ensureLogin`; блокер — нет кредов (владелец: «нет доступа»). Публичного API «все заказы» у tradesk нет (`/api/orders` только по `client_id` одного клиента)
- [x] Экран «Товары» (read-only + тумблеры Скрыт/Бейдж «Хит»)
- [x] **Проверено end-to-end на дев-стеке** (логин, гард, рендер, оверрайды)

### Инфра / прочее
- [x] Docker Compose (postgres:5432, api:8080, web:3000) + hostnet-workaround
- [x] `.gitignore`, README, PROJECT_OVERVIEW.md, ARCHITECTURE.md
- [x] Граф проекта (graphify)

---

## 🟡 Ближайшее

### ⭐ ПРИОРИТЕТ 18.08.2026 — правки владельца (9 пунктов, опора на старый сайт tirestock.ru)
- [x] #1 Пункты выдачи — реальные 20 адресов со старого сайта (`/points/`) вместо демо (миграция 0015: slug+is_main+city, 20 точек СПб)
- [x] #2 Обратная интеграция tradesk (статусы заказов НА сайт) СДЕЛАНА: контракт вскрыт по коду старого сайта `/status/index.php` (эндпоинт `GET tradesk.ru/data/status?order_code=`). Пакет `internal/orderstatus` (Source-интерфейс + Service с маппингом статусов 1:1 со старым сайтом + `GET /order-status?code=`), реализация `integrations/tradesk/status.go`, заглушка `Disabled` когда tradesk выключен. Витрина `/status/` (URL 1:1, форма + результат + полоса прогресса + состав заказа), футер «Что с моим заказом?». SEO-мета `/status` (миграция 0016). Юнит-тесты маппинга зелёные. Проверено: страница/API работают, `found:false` при неизвестном номере (валидный активный номер для успешного кейса — за владельцем)
- [x] #3 Выбор в корзине способа получения СДЕЛАНО (по макету старого сайта): радио «Доставка курьером» + самовывоз из каждого пункта города (`CheckoutFulfilment`), ссылка «на карте» → модалка Яндекс-карты. Поля Ф.И.О.*/телефон*/способ* обязательны, адрес* — только при доставке, e-mail — опционально, город — из переключателя. Способ+адрес+город+email собираются в комментарий заказа (в addorder tradesk отдельных полей нет — контракт /ajax/order.php). Пункты грузятся по городу. Проверено сквозняком: заказ #31 → comment «Самовывоз: Полюстровский пр., 73. Город: … E-mail: …», запрос к tradesk сформирован верно (доставка через outbox+ретраи; сейчас сеть/VPN рвёт исходящее — заказ не теряется)
- [x] #4 Вернуть Москву (СПб + Москва, БЕЗ 60 поддоменов) СДЕЛАНО: восстановлен city-aware слой (миграция 0017 — `product_offers` заново, бэкфилл СПб из products; отменяет 0009). Синк агрегирует цену/остаток по каждому городу (`CityStocks` map, СПб + Москва по подстрокам складов), пишет офферы. Каталог/товар/фасеты city-aware (INNER JOIN `product_offers` по городу, `?city=spb|msk`; нет оффера в городе → товар скрыт/404). Витрина: кука города + переключатель `CitySwitcher` в шапке + `/api/city`, город прокинут в каталог/товар/главную/пункты/счётчик; шапка city-aware (адрес/телефон/label). Пункты Москвы — empty-state до данных. Проверено: СПб-каталог 8504 товара, переключение города, миграция. Москва наполнится на следующем УСПЕШНОМ синке (сейчас ссылка фида протухла — известная ротация SelectTyres, не баг). Юнит-тесты синка/фильтров зелёные
- [ ] #5 Пообщаться с SEO-специалистом (за владельцем)
- [x] #6 Несколько основных адресов (Полюстровский, Торфяная дорога, Новосёлов) — флаг `is_main`, крупные карточки с телефоном первыми
- [x] #7 Карточка пункта кликабельна → отдельная страница `/points/<slug>` (карта, часы, телефон); «Подробнее» ведёт туда же
- [x] #8 Сортировка каталога «По названию» → «По алфавиту»
- [x] #9 Убрана покраска дисков из строки преимуществ + преимущества кликабельны (ведут на страницу сервиса; ссылка `href` редактируется в админке, миграция 0014)

### ⭐ ПРИОРИТЕТ 11.08.2026 — обязательные инфо-страницы (5 пунктов)
Контент снят со старого сайта (анализ: scratchpad `oldsite-content-analysis.md`).
Пункт «форма оплаты картой» не актуален — эквайринга не будет.
- [x] Разведка старого сайта: /delivery/, /payment/, /warranty/, /contacts/, /about/, /points/, /status/
- [x] Дизайн новых страниц в Figma — секция «05 · Обязательные инфостраницы» (фреймы 117:605–608)
- [x] Вёрстка: `/delivery/` (+экспортные ограничения = только РФ), `/payment/` (+процедура заказа, без онлайн-оплаты), `/warranty/` (гарантия+возврат), `/contacts/` (city-aware телефон/адрес, реквизиты)
- [x] URL 1:1 со старым сайтом; демо-сиды `/dostavka/`, `/info/oplata/`, `/garantiya/` удалены (миграция 0007)
- [x] Навигация: Footer → новые URL + «Контакты»; Header nav + «Контакты»; «Что с моим заказом?» → tel: (был href="#"-тупик); аудит всех внутренних ссылок — 404 нет
- [x] **Проверено на дев-стеке:** все 4 страницы 200, старые URL со слэшем → 308 на без-слэшевые, удалённые сиды → 404
- [ ] ❓ Виталию: какие реквизиты актуальны — ООО «ВИТАСФЕРА» (/contacts/) или ИП Бунин (/about/)?
- [x] Trailing slash ВКЛЮЧЁН (07.09, правка сеошника): `trailingSlash:true` — URL со слешем + 301 (как старый сайт); Next-api вызовы (city/catalog-count) переведены на слеш. Плюс SEO-мета фильтров каталога (lib/catalog-seo.ts: уникальные H1/Title/Desc, count убран из H1, canonical+noindex для noise), SEO-текст каталога, фикс сброса фильтра, футер-перелинковка, /news/ и /partnership/ перенесены
- [ ] Страница проверки статуса заказа (/status/ на старом сайте) — пока «Что с моим заказом?» ведёт на tel:

- [ ] ⭐ **СЛЕДУЮЩАЯ:** интеграция SelectTyres — источник товаров/остатков (см. раздел «Каталог и данные»; блокер — механизм отдачи данных)
- [x] 🎉 **ИНТЕГРАЦИЯ tradesk ВСКРЫТА ПОЛНОСТЬЮ (11.08).** Все 12 `/ajax/*.php` прочитаны и разобраны → `docs/OLDSITE_AJAX.md` (назначение + исходники). Битрикс оказался тонким прокси: заявка — `GET /data/backcall?phone=&comment=`, заказ — `GET /api/addorder?…` (одна позиция на вызов, ответ = номер заказа либо `error`), без авторизации и CSRF
- [x] Адаптеры переписаны на реальный контракт: `integrations/tradesk` (прямой) и `integrations/oldsite` (мост) — оба умеют и заявку, и **заказ** (раньше заказ не умел никто); догадочные Yii2-поля удалены; тесты зелёные
- [ ] ❓ Виталию: `code` в приёмнике — код старого магазина (99302), у нас SelectTyres (`t668559`). Принимает ли tradesk наши коды или нужна карта соответствия (её знает импорт в Битрикс)?
- [x] **Живая проверка заказа на боевом tradesk пройдена (11.08, с разрешения):** заказ #22 «ТЕСТ — не обрабатывать», тел. +7 000 000-00-00, 1 ₽, код `t499609` прошёл весь путь `POST /api/v1/orders` → outbox → воркер → `GET tradesk.ru/api/addorder` и доставлен с первой попытки (`delivered`, 0 ретраев). Дев-стек возвращён на мок (`TRADESK_BASE_URL=` пуст)
- [x] **Формат ответа приёмника подтверждён на живом заказе (11.08):** заказ #25 (корзина, 4 шт. Cordiant по 5 130 ₽, код `t499559`) → tradesk вернул номер **`C288414`**; он сохранён в `outbox.result` и виден в админке. Дев-стек снова на моке
- [ ] Убедиться в tradesk, ЧТО долетело в записях #22/#25: читаемость кириллицы (product/name/comment) и как принят наш код `t499559` (поле code) — это же ответ на вопрос о пространствах кодов
- [ ] `/api/record` (онлайн-запись: point_id/date/time/promo) — подключить к формам сервисных страниц, когда добавим выбор даты/времени
- [ ] Калькулятор доставки в регион на карточке товара (`pec.php` → `calc.pecom.ru`, отправитель СПб `town=-463`) + справочник PEC_ID городов (`city.php`)
- [ ] Отзывы о шинах: на старом сайте это SQL-таблица `review` в БД Битрикса (`model_id`, рейтинг, schema.org) — нужна выгрузка для страницы «Отзывы» и карточки товара

### ✅ Мультигород ОТМЕНЁН и ВЫЧИЩЕН (11.08.2026)
Только Санкт-Петербург, городских поддоменов не будет.
- [x] Удалены `CitySwitcher`, `lib/city.ts`, `lib/get-city.ts`, `app/api/city/route.ts` → одна константа `web/lib/shop.ts` (`SHOP`)
- [x] Параметр `city` убран из API/OpenAPI, `Filters.City`, `CatalogSource.BySlug`; каталог читает цену/остаток прямо из `products`
- [x] Синк считает только склады СПб (`StockFilter`), товары чужих городов в каталог не попадают
- [x] Миграция 0009: снапшот цен СПб в `products`, удаление товаров без предложения в СПб, `DROP TABLE product_offers`
- [x] `SELECTYRES_STOCK_MSK` убран из env и compose
- [x] **Проверено на живых данных:** каталог 13 351 → 6 487 товаров (ровно ассортимент СПб), витрина и карточка товара работают
- [ ] Первый коммит в git (репозиторий ещё без коммита кода)
- [ ] Ротировать пароль/сессию tradesk (кука светилась в чате при разведке)

---

## ⏳ Бэклог

### ✅ Интеграция — Вариант 2 (прямой tradesk, минуя Битрикс) — СДЕЛАН 11.08.2026
- [x] Приёмники вскрыты: `/data/backcall`, `/api/addorder`, `/api/request` (+ `/api/record`, `/api/gift` в запасе)
- [x] Реальные поля в `integrations/tradesk` вместо догадок; заказ, обратный звонок и заявка доставляются
- [x] Контракт доставки **заказа** вскрыт (`order.php` + фронт карточки): одна позиция на вызов, ответ = номер заказа
- [x] Битрикс выведен из цепочки: приоритет доставки — прямой tradesk, мост `oldsite` остаётся аварийным (лог WARN)
- [x] Номер записи tradesk сохраняется (`outbox.result`, миграция 0008) и виден в админке — без дублирования учётки
- [ ] На проде задать `TRADESK_BASE_URL=http://tradesk.ru` (и НЕ задавать `OLDSITE_BASE_URL`)

### Каталог и данные (SelectTyres)
- [x] Механизм вскрыт: единый JSON-фид (ассортимент+остатки), ~43МБ, раз в час
- [x] Адаптер `integrations/selecttyres` (стриминг-парс, агрегация СПб) + тесты
- [x] Синк-воркер (`time.Ticker`) → upsert в read-модель `products` (миграция 0003)
- [x] DB-источник каталога (`catalog.DBSource`) вместо мока + скрытие оверрайдов
- [x] **Проверено:** 16 959 в фиде → 10 615 с СПб-наличием в каталоге; API/витрина/админка отдают реальные товары
- [x] Реальные фото товаров из фида — отрисованы на карточке, странице товара, главной
- [x] Текстовый поиск (hero «Поиск по каталогу») — `q` в API/фильтрах + витрина
- [x] Живое число «Показать N шин» на кнопке фильтра и hero (route `/api/catalog-count`)
- [x] Правило цены: **РРЦ** (Виталий подтвердил 24.07) — синк агрегирует по петербургским складам
- [ ] 🔴 ДИСКИ (каталог /wheels/) — отдельный ТИП товара, не «просто ещё одна выгрузка». Диски у того же SelectTyres: в фиде есть секция `wheels` (сейчас синк её пропускает, client.go:127), но она ПУСТА — Виталий должен включить выгрузку дисков в кабинете SelectTyres. Что построить: (1) поле category (шина|диск) + хранение атрибутов дисков (диаметр, ширина в дюймах, PCD/сверловка, вылет ET, DIA, цвет, литьё/ковка); (2) парсер секции wheels в синке; (3) каталог /wheels/ со своими фильтрами; (4) карточки/страница дисков; (5) мультигород по городам как у шин. Начать после того как секция wheels наполнится — тогда спроектировать схему под реальные поля фида. Общий мультифид (второй поставщик) — тоже возможен (доп. Source + дедуп по коду), делаем при появлении

### Мультигород (СПб/МСК) + чистые фото — фича 24.07 (ветка feature/multicity-clean-photos)
- [x] Модель `product_offers(product_code, city, price, stock)` + `products.image_clean_url` (миграция 0004)
- [x] Синк агрегирует цену/остаток **по каждому городу** (СПб/МСК), фильтры складов из env
- [x] Каталог/товар city-aware (`?city=`, INNER JOIN offers, нет в городе → скрыт); OpenAPI + типы
- [x] Avito-синкер чистых фото (без вотермарка) → `image_clean_url` (эффективное фото = clean ∥ токенное)
- [x] Кука города (DNS-стиль) + переключатель в шапке (адрес/телефон по городу)
- [x] Бэкенд собирается, все Go-тесты + tsc зелёные
- [x] **Живая проверка пройдена (свежий фид 19bdd7af):** синк наполнил оба города — СПб ~6.2к, МСК ~2.7к товаров; витрина/переключатель работают
- [x] МСК-фильтр сверен с реальными складами: `moskva,moscow` (БЕЗ `msk` — склад `sever-avto-msk_sankt-peterburg` физически питерский)
- [x] **Живучесть каталога:** сид `product_offers(СПб)` из снапшота `products` (миграция 0005) + порог здоровья синка (усохший фид не сносит каталог, env `SELECTYRES_MIN_HEALTHY_RATIO`) + `products.price/stock` ведётся как денорм-снапшот
- [x] Хардкод «СПб» на странице товара → city-aware (падеж `loc`)
- [x] Счётчик «Показать N шин» берёт город из куки (`/api/catalog-count`)
- [x] 🐞 Исправлен сломанный фильтр цены: `WhereSQL` ссылался на `po.price` (алиас удалённой мультигород-таблицы `product_offers`), запрос падал 500 при `price_min/price_max`. Теперь `price` (колонка в `products`). Тест `filters_test` обновлён (закреплял баг)
- [x] Фильтр наличия в каталоге (`?stock=in|out`, `Filters.Availability`, WhereSQL `stock > 0`/`= 0`): распроданные товары синк не удаляет (обнуляет остаток), но их было не найти — сортировка гонит наличие вверх. В админ-«Товары» вкладки «Все / В наличии / Нет в наличии» (потолок выборки 1000, чтобы все ~505 распроданных попали). Проверено: `out` → 505 распроданных
- [ ] Юнит-тест на порог здоровья синка (сейчас `Fetch` — конкретный тип; вынести за интерфейс)
- [ ] Ссылки на выгрузки SelectTyres ротируются/протухают → вопрос в поддержку: постоянный URL/API?
- [ ] Условный GET фида (If-Modified-Since) — не качать ~43МБ, если не менялся
- [ ] SEO-slug: сверить URL со старым сайтом (сейчас slug = имя+код)
- [x] Отрисовать бейдж «Хит» на карточке витрины и странице товара (`badge_hit`)
- [x] Состояние товара «нет в наличии» (серый бейдж) — карточка + товар. Товар НЕ пропадает при обнулении остатка: синк `ZeroStaleStock` обнуляет, а не удаляет (строки живут — ссылки заказов + SEO-URL), каталог отдаёт их в конце списка
- [x] Кнопка «Уточнить при наличии» вместо отключённой CTA на нет-в-наличии (карточка + страница товара): переиспользован `CallbackModal` (обобщён под link/button + заголовок/описание/предзаполненный товар), заявка с артикулом уходит в tradesk «Обратный звонок» через outbox. Проверено сквозняком
- [x] Админ-«Товары» переоформлены понятнее (read-only не очевиден из-за ползунков): read-only плашка «данные из SelectTyres, редактировать нельзя», колонка «Оверрайды»→«Показ на витрине», тумблеры с видимыми подписями и позитивной формулировкой «Показывать в каталоге» / «Бейдж «Хит»»
- [x] Сортировка каталога (`?sort=price_asc|price_desc|name`, наличие всегда сверху; белый список `SortOrders` — без SQL-инъекции; `SortSelect`)
- [x] Реальные опции фильтра (бренды/размеры) из данных вместо статики — `GET /catalog/facets` (`array_agg DISTINCT`), фолбэк на `catalog-options` при пустом фасете/ошибке
- [x] Всё подтягивается из выгрузки SelectTyres, а не хардкод (заказчик ленивый): hero-поиск (`SearchWidget`) переведён на фасеты — ширина/профиль/диаметр из данных (ширины >255 появляются сами: 265…335), чипы «Популярно» = топ типоразмеров по наличию (`popular_sizes` в фасетах). Фасеты почищены санитарными диапазонами (`MinWidth..MaxWidth` и т.д.) — мусор фида (width=7 и т.п.) не попадает в выпадашки. Сезон остаётся статикой (закрытый enum). Проверено: hero и каталог-фильтр из данных, мусор убран
- [ ] Таб «По авто» — база подбора SelectTyres (`SearchWidget`). ВРЕМЕННО СКРЫТ (26.08, фича не готова): убран из hero-табов, дропдауна «Шины», подзаголовка/SEO-описаний. Вернуть — добавить "По авто" обратно в `TABS` + блоки формы/подсказки (см. коммент в SearchWidget)
- [ ] Сверить цену заказа с каталогом (`orders.go`) — сейчас цена берётся из каталога на checkout, но не перепроверяется в момент доставки

### Корзина и заказ
- [x] Настоящая корзина: кука + серверное чтение (`web/lib/cart.ts`), server actions, счётчик в шапке
- [x] Добавление в корзину с дефолтным количеством 4 (`AddToCart`, `ProductCard`)
- [x] Пересчёт сумм при изменении количества (`CartQty` → server action, цена из каталога, не из куки)
- [x] Удаление из корзины — undo-снекбар (не confirm, не мгновенно)
- [x] Оформление заказа → `POST /orders` (имя + телефон + комментарий; код товара берётся с сервера) → outbox → tradesk
- [x] 🐞 Исправлен баг идемпотентности: ключ выводился из телефона+состава, поэтому ПОВТОРНЫЙ заказ тех же шин на тот же телефон молча возвращал старый заказ и в tradesk ничего не уходило. Теперь ключ рождается вместе с формой (скрытое поле) — даблклик защищён, повторная покупка проходит. Проверено браузером: два одинаковых заказа → `C288416` и `C288417`
- [x] Живая проверка корзины на боевом tradesk пройдена (заказ #25 → номер `C288414`)
- [x] Живая проверка заявки с формы услуги (`/api/request`) пройдена — долетела в tradesk с пометкой стенда
- [x] Локальный стенд переключён на боевой tradesk (`TRADESK_BASE_URL` + `DELIVERY_TEST_MARK` в `deploy/.env`)
- ~~Эквайринг~~ — ОТМЕНЁН (Виталий, 11.08.2026: платёжки на сайте не будет)

### Витрина / UI
- [x] Выпадающие панели Шины/Диски/Сервис (`NavDropdown`: hover-мост, Enter/Escape, aria, анимация)
- [x] Оптимизация фото: next/image (webp+ресайз+lazy) на карточке, товаре, главной
- [x] Главная: ISR — «Популярные» берут свежий каталог (не мок из билда)
- [x] Аудит анимаций: кнопки (нажатие+цвет), hover фото, анимация дропдаунов, prefers-reduced-motion
- [x] Мобильное меню-бургер (Figma 99:764): аккордеон-меню < lg, CTA-телефон, адрес/часы по городу
- [x] Сервисные страницы (11.08.2026, Figma «05 · Обязательные инфостраницы»): `/mounting/`, `/disk-repair/`, `/disk-painting/`, `/motomounting/` — цены со старого сайта, общая форма «Онлайн запись» (`ServiceSignup` → callbacks → outbox), таблицы `PriceTable` (overflow-x, tabular-nums)
- [x] Шинный калькулятор `/tyres-calc/` (клиентский `TyreCalc`, порог 3% диаметра)
- [x] Тупики развязаны: ServicesSection и меню «Сервис» ведут на реальные страницы; `href="#"` в коде витрины не осталось
- [x] Страница «Пункты выдачи» свёрстана по макету Figma (99:536) вместо текст-заглушки: кодовый роут `(storefront)/points`, данные в `lib/pickup-points.ts` (демо из макета — реальный список 34 пунктов даёт Виталий), центральный склад + сетка карточек (адрес/метро/часы/«−15%»/«Подробнее»→Яндекс.Карты), SEO-мета `/points` (миграция 0012: удалила осиротевшую системную контент-страницу `/points/`, завела seo-роут). Проверено сквозняком
- [x] Живая карта Яндекс.Карт на «Пунктах выдачи» (виджет-iframe без API-ключа, центр — склад) вместо серого плейсхолдера. Хвост: мультипин по всем пунктам — с координатами от Виталия (карта-конструктор)
- [x] Раздел «Пункты выдачи» в админке СДЕЛАН: таблица `pickup_points` (миграция 0013 + сид из статики), пакет `internal/pickups` (Service + публичный/админ-хендлеры), CRUD за сессией, `GET /pickup-points` в OpenAPI. Витрина `(storefront)/points` тянет пункты из API (no-store, фолбэк на статику `lib/pickup-points`), счётчики/склонение — из данных. Фронт админки: список + форма (адрес/метро/часы/**свой бейдж-акция у каждого**/центральный склад/порядок/публикация). Проверено сквозняком: CRUD, индивидуальный бейдж (Советский → «−20% на хранение»), публичный API отражает мгновенно. Решает «у всех одна и та же скидка»
- [x] Главный адрес обновлён: Зотовский пр. 11 → **Петровская коса** (владелец, 17.08.2026 — точки на Зотовском больше нет). Правки: `SHOP.address` (шапка/мобильное меню), Footer, страницы шиномонтажа/контактов, центральный склад на «Пунктах выдачи»
- [x] Услуги сокращены до реальных двух (владелец: у заказчика только хранение + шиномонтаж): убраны плитки/пункты меню/страницы «Ремонт дисков» (`/disk-repair/`), «Покраска дисков» (`/disk-painting/`), «Мотошиномонтаж» (`/motomounting/`) — роуты удалены (→404), `ServicesSection` переверстан в 2 равные featured-карточки
- [ ] Раздел «Диски» СКРЫТ из шапки до включения выгрузки дисков в SelectTyres (вернуть nav-блок в `Header.tsx`, когда появятся данные)
- [ ] Реальное фото склада для страницы «Хранение» (от клиента)
- [ ] Цены за сезон хранения — плейсхолдеры, уточнить у Виталия

### Админка — Фаза 2 (контент)
- [x] Авторизация (страницы в том же Next) — сделано в Фазе 1
- [x] Оверрайды товаров (скрыть/бейдж) поверх read-модели — сделано в Фазе 1
- [x] Раздел «Страницы» СДЕЛАН: таблица `content_pages` (миграция 0006) + пакет `internal/content` (правила блокировки indexed/system) + CRUD-эндпоинты за сессией + фронт (список 100:604, редактор 93:757, создание). Проверено сквозняком.
- [x] Раздел «Преимущества» СДЕЛАН: таблица `benefits` (миграция 0010 + сид из хардкода), пакет `internal/benefits` (Service + публичный/админ-хендлеры), CRUD за сессией, `GET /benefits` в OpenAPI. Витринный `BenefitsBar` тянет офферы по API (no-store, фолбэк на статику). Фронт админки: список + форма (создание/правка/удаление, порядок, скрытие). Проверено сквозняком на живой БД: CRUD, сортировка по `sort_order`, скрытый оффер исключён из публичного `/benefits`, рендер витрины и админки
- [x] Раздел «SEO-мета» СДЕЛАН: таблица `seo_meta` (миграция 0011 + сид из кода: дефолт-шаблон `*`, `/`, `/catalog`), пакет `internal/seo` (Service + резолв «роут ∥ дефолт по полям» + публичный `GET /seo?path=` + админ `GET/PUT /admin/seo`), OpenAPI `SeoResolved`. Витрина: `generateMetadata` главной и каталога тянут мету из админки (`lib/seo.ts`, ревалидация 300с — ISR сохранён, не no-store). Фронт админки: карточки маршрутов с Title/Description (счётчики 60/160). Проверено сквозняком: CRUD, публичный резолв + фолбэк на дефолт, `<title>` каталога/главной из меты, рендер админки
- [x] Рендер контентных страниц на витрине по их URL СДЕЛАН: публичный `GET /content?path=`, catch-all `(storefront)/[...slug]`, SEO-мета из админки, no-store (правки видны сразу). Ссылки в Footer/шапке ведут на реальные URL. Черновики → 404.
- [x] Таблица `benefits` + пакет `internal/benefits` (сделано, см. выше)
- [x] Оверрайды применены на витрине: скрытый товар не отдаётся (`DBSource` фильтрует `hidden`), бейдж «Хит» рисуется (карточка + товар)
- [ ] Синхронизировать admin-эндпоинты в `openapi.yaml` (сейчас клиент `lib/api/admin.ts` ручной)

### SEO (священная корова)
- [ ] 🔴 Сверить структуру URL со старым сайтом 1:1 (или 301 на каждый)
- [x] robots.txt Disallow + noindex на тестовом домене: fail-safe гейт `SITE_INDEXABLE` (по умолчанию ЗАКРЫТО). `app/robots.ts` (Disallow `/` вне боевого), middleware вешает `X-Robots-Tag: noindex, nofollow` на все ответы. На боевом домене `SITE_INDEXABLE=true` открывает сайт (кроме `/admin`,`/cart`,`/dev`). Env задокументирован в compose + `.env.example`
- [ ] Проверка индексируемых страниц перед переключением домена

### Города — СПб + Москва (25.08.2026, БЕЗ поддоменов)
СПб-only отменён владельцем: вернули Москву (два города, переключатель + кука,
цены/остатки/пункты по городу). Городских ПОДДОМЕНОВ по-прежнему нет — разбор ~60
городов старого сайта и middleware поддоменов не нужны. См. #4 выше.

### Инфра / запуск
- [x] 🐞 Кэш фото в контейнере (11.08): Next не мог писать `/app/.next/cache` (образ от root, сервер от node) и пережимал каждое фото заново. Каталог создаётся в Dockerfile с правами `node`, вынесен в том `web-image-cache`, TTL 31 день, потолок 512 МБ (LRU), урезаны размеры вариантов. Проверено: повтор — `X-Nextjs-Cache: HIT` (1.1с → 0.003с), слой контейнера 0 Б, кэш переживает пересоздание контейнера
- [x] Демо-стенд для заказчика через Cloudflare quick-туннель (17.08): весь стек на локальной машине + `cloudflared tunnel --url` → публичный HTTPS `*.trycloudflare.com`. Скрипт `deploy/demo-cloudflare.sh` (стек+туннель одной командой). Overlay `docker-compose.hostnet-demo.yml` (api/web на host-сети — обход сломанного hostnet-workaround). Плагин Cloudflare для Claude Code поставлен. Проверено сквозняком: главная/каталог(6.9к товаров)/контент-страницы/футер/админ — 200, гейт индексации закрывает демо. URL меняется при перезапуске (quick-туннель без аккаунта)
- [ ] Починить firewall для межконтейнерного трафика (bridge FORWARD dropped; overlay host-сети — временный обход, `iptables -P FORWARD ACCEPT`/правило для docker уберёт нужду в нём)
- [ ] nginx/traefik в compose
- [ ] Прод-деплой на VPS в РФ (стабильный named-туннель Cloudflare со своим доменом ИЛИ обычный reverse-proxy)
- [ ] На проде включить боевой мост (`OLDSITE_BASE_URL=https://tirestock.ru`)

---

## Открытые вопросы к Виталию
Полный список и способы проверки — `docs/ARCHITECTURE.md → Открытые вопросы`.
Актуальные вопросы к владельцу:
- Реквизиты: ООО «ВИТАСФЕРА» (/contacts/) или ИП Бунин (/about/)?
- Коды товаров: принимает ли tradesk наши коды SelectTyres или нужна карта соответствия?
- Диски: включить выгрузку в кабинете SelectTyres (в фиде сейчас 0).
- Хранение: реальное фото склада + цены за сезон.
- Пункты выдачи: реальный список 34 пунктов (адреса/метро/часы/акции) + координаты для карты Яндекс.Карт (сейчас демо из макета в `lib/pickup-points.ts`).
- Прод: доступ к VPS в РФ для деплоя.
Закрыто: ~~доступ к call.php~~ (прочитан через админку Битрикса), ~~механизм SelectTyres~~ (фиды),
~~устройство поддоменов~~ (мультигород отменён), ~~эквайринг~~ (отменён).
